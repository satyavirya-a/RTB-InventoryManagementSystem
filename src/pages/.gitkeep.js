/**
 * @folder src/pages/
 *
 * Berisi komponen halaman — satu file = satu halaman/route aplikasi.
 *
 * Rencana halaman yang akan dibuat:
 * - CatalogPage.jsx    → Halaman utama: daftar semua barang aktif
 * - TransactionPage.jsx → Form checkout (pilih jenis transaksi, isi cart, upload foto)
 * - HistoryPage.jsx    → Riwayat transaksi
 * - AdminPage.jsx      → (Admin only) Kelola barang, koreksi stok
 *
 * Perbedaan Pages vs Components:
 * - Pages: terhubung ke URL/route, bisa fetch data sendiri, biasanya satu per route
 * - Components: unit kecil reusable, tidak tahu soal routing/URL
 */

export {}
