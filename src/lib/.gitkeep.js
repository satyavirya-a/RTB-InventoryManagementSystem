/**
 * @folder src/lib/
 *
 * Berisi kode "infrastruktur" dan utilitas — bukan komponen UI.
 * File di sini biasanya tidak ada elemen JSX sama sekali.
 *
 * File yang akan dibuat:
 * - supabaseClient.js    → Inisialisasi koneksi ke Supabase (Fase 2)
 * - imageCompression.js  → Fungsi kompresi gambar sebelum upload (Fase 6)
 * - constants.js         → Nilai tetap: nama event, jenis transaksi, dll
 *
 * Analogi:
 * Kalau komponen adalah "mesin kopi", maka lib/ adalah "saluran air dan listrik"
 * yang menghubungkan mesin ke sumber daya. Penting tapi tidak terlihat langsung.
 */

export {}
