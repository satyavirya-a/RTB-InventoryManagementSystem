/**
 * @folder src/components/
 *
 * Berisi komponen UI yang bisa dipakai ulang (reusable) di banyak halaman.
 *
 * Contoh komponen yang akan dibuat di fase selanjutnya:
 * - ItemCard.jsx       → Kartu tampilan satu barang di katalog
 * - CartDrawer.jsx     → Panel keranjang yang slide dari kanan
 * - QuantityInput.jsx  → Input angka dengan tombol +/-
 * - StatusBadge.jsx    → Badge "Tersedia", "Habis", "Dipinjam"
 *
 * Aturan folder ini:
 * - Satu file = satu komponen utama
 * - Nama file = PascalCase (ContohKomponen.jsx), sama seperti nama komponen
 * - Komponen di sini TIDAK boleh langsung fetch data — data dikirim via props
 */

// File ini hanya placeholder — akan dihapus saat komponen pertama dibuat.
export {}
